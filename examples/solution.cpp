#include <iostream>
#include <string>

int main() {
    // Simple program that reads input and produces output
    // Example: reads two integers and outputs their sum
    int a, b;
    std::cin >> a >> b;
    std::cout << a + b << std::endl;
    return 0;
}
